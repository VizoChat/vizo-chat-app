import { StringToUrlConvertPipe } from './string-to-url-convert.pipe';

describe('StringToUrlConvertPipe', () => {
  it('create an instance', () => {
    const pipe = new StringToUrlConvertPipe();
    expect(pipe).toBeTruthy();
  });
});
