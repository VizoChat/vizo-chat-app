import { GoogleSigninButtDirective } from './google-signin-butt.directive';

describe('GoogleSigninButtDirective', () => {
  it('should create an instance', () => {
    const directive = new GoogleSigninButtDirective();
    expect(directive).toBeTruthy();
  });
});
